info:
  name: 04. Query Events - All Events
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/audit/events"
  params:
    - name: page
      value: "0"
      type: query
    - name: size
      value: "20"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  Test Case 4: Retrieve all audit events with no filters, paginated.
  Expected: 200 OK, content array with all 3 events created so far.
